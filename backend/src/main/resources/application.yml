server:
  port: 8080

spring:
  profiles:
    active: local
  data:
    mongodb:
      uri: "mongodb+srv://navya:navya@ac-3odxllo.qwfarwq.mongodb.net/nebula?authSource=admin&retryWrites=true&w=majority"
      database: nebula
  mail:
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USER:${MAIL_USERNAME:navyaneelam115@gmail.com}}
    password: ${SMTP_PASS:${MAIL_PASSWORD:gqbc nqrh emdy qzop}}
    properties:
      mail:
        debug: true
        smtp:
          auth: ${SMTP_AUTH:true}
          starttls:
            enable: ${SMTP_STARTTLS:true}
          ssl:
            trust: ${SMTP_SSL_TRUST:smtp.gmail.com}

app:
  jwt:
    # A standard 256-bit base64-encoded secret key for local fallback.
    secret: ${JWT_SECRET:dGhlLXNlY3JldC1rZXktZm9yLW5lYnVsYS1hdXRoLW11c3QtYmUtYXQtbGVhc3QtMjU2LWJpdHMtbG9uZy1hbmQtc2VjdXJlIQ==}
    expiration-ms: 900000 # 15 minutes

# default from address (root-level env override)
mail-defaults:
  from: ${MAIL_FROM:navyaneelam115@gmail.com}
